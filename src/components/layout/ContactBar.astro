<div class="contact-bar">
  <button type="button" class="contact-link" data-contact-copy="info@corecreative.jp">
    <span class="text-zinc-500">CONTACT:</span>
    <span id="contact-text">info@corecreative.jp</span>
    <span id="copy-toast" class="copy-feedback">COPIED</span>
  </button>
</div>

<style>
  .contact-bar {
    display: flex;
    justify-content: flex-end;
    padding: 10px 20px;
    font-size: 10px;
    letter-spacing: 0.2em;
    border-bottom: 1px solid rgba(34, 211, 238, 0.1);
    position: relative;
    z-index: 10;
  }

  .contact-link {
    color: #71717a;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 8px;
    transition: color 0.3s ease;
    background: transparent;
    border: 0;
    font: inherit;
    letter-spacing: inherit;
    padding: 0;
  }

  .contact-link:hover {
    color: #22d3ee;
  }

  .copy-feedback {
    position: absolute;
    right: 0;
    top: 120%;
    background: #22d3ee;
    color: #000;
    padding: 2px 6px;
    font-weight: bold;
    opacity: 0;
    transition: opacity 0.3s ease;
    pointer-events: none;
  }

  .copy-feedback.is-visible {
    opacity: 1;
  }
</style>
